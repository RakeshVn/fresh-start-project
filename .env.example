# MongoDB Atlas connection string (optional - falls back to in-memory storage for development)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/flapstr

# Backend server port (default: 3001)
# PORT=3001

# Backend API target for Vite dev proxy (default: http://localhost:3001)
# VITE_API_TARGET=http://localhost:3001
